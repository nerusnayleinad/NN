apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
 
namespace: prod

labels:
- pairs:
    version: v1
    env: prod
 
patches:
- path: deploy.yaml
- path: hpa.yaml

commonAnnotations:
  env: prod

resources:
- ../../base